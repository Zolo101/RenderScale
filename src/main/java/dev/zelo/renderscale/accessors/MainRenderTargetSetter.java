package dev.zelo.renderscale.accessors;

import com.mojang.blaze3d.pipeline.RenderTarget;

public interface MainRenderTargetSetter {
    void renderScale$setMainRenderTarget(RenderTarget renderTarget);
}
